# AI-Assisted Analysis Prompt — Hunt 15

Use only data approved for the selected AI system. Treat the output as proposed analysis and verify every material claim against the underlying evidence.

```text
Build two synchronized timelines: endpoint artifact access and identity/application activity.
Identify each device, client, IP category, geography, application, session, token, request, and correlation identifier.
Mark whether a fresh primary and MFA authentication sequence exists before each activity cluster.
Separate missing device data from changed device data.
List benign network and virtual-desktop explanations.
State the minimum evidence still needed before calling this token replay.
```
