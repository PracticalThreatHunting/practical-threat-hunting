# AI-Assisted Analysis Prompt — Hunt 14

Use only data approved for the selected AI system. Treat the output as proposed analysis and verify every material claim against the underlying evidence.

```text
Reconstruct this user's authentication and factor lifecycle in chronological order.
For every step, show method, result, source, device, application, actor, and correlation identifier when present.
Mark missing fields explicitly.
Identify the first successful session after repeated failures or denials.
Compare the method and device with the user's 30-day baseline.
List benign recovery and stale-session explanations.
Do not label a push approval malicious solely because it followed denied prompts.
```
