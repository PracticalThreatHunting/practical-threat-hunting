# AI-Assisted Analysis Prompt — Hunt 18

Use only data approved for the selected AI system. Treat the output as proposed analysis and verify every material claim against the underlying evidence.

```text
Analyze these role and group changes as actor-target-privilege relationships.
Use stable object IDs and keep user and application actors distinct.
Identify self-grants, rare actor-to-role pairs, permanent changes where temporary access is normal, and sensitive actions after activation.
Compare each event with the supplied approved workflow and inventory.
List missing approvals or ownership records as governance gaps, not proof of compromise.
```
