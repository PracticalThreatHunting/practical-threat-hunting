# AI-Assisted Analysis Prompt — Hunt 16

Use only data approved for the selected AI system. Treat the output as proposed analysis and verify every material claim against the underlying evidence.

```text
Compare these recent sign-ins with the user's historical context.
Use geography, network category, device, client, application, authentication method, and post-login actions.
Treat IP geolocation and proxy labels as fallible enrichment, not ground truth.
Exclude comparisons between non-interactive service activity and human travel unless justified.
List the exact combinations that are new, the benign explanations, and the evidence needed to raise confidence.
```
