# AI-Assisted Analysis Prompt — Hunt 12

Use only data approved for the selected AI system. Treat the output as proposed analysis and verify every material claim against the underlying evidence.

```text
Correlate this endpoint timeline with the affected user's sign-in and application-activity timeline.
Identify discontinuities in device ID, client, IP ownership category, geography, session or correlation ID, and resource access.
Separate fields that are missing from fields that changed.
List benign mobility, proxy, and device explanations.
Do not claim token replay unless the evidence supports use of an existing or stolen session rather than a new legitimate authentication.
```
