# AI-Assisted Analysis Prompt — Hunt 06

Use only data approved for the selected AI system. Treat the output as proposed analysis and verify every material claim against the underlying evidence.

```text
Using this approved, redacted event set, identify non-browser processes that accessed or staged browser and application credential-store artifacts.
Group known enterprise tools separately from rare processes.
For each rare process, list ancestry, signer, prevalence, files touched, archives created, and identity activity that followed.
Do not expose or reproduce token, cookie, password, or personal-data contents.
Do not infer successful theft from file access alone.
```
