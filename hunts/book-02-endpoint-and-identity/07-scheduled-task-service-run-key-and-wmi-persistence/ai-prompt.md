# AI-Assisted Analysis Prompt — Hunt 07

Use only data approved for the selected AI system. Treat the output as proposed analysis and verify every material claim against the underlying evidence.

```text
Compare these new or modified tasks, services, run-key values, and WMI subscriptions with the supplied 30-day baseline.
Normalize variable IDs and timestamps but preserve the original event IDs.
Rank entries by rare creator, privileged run context, user-writable target, interpreter use, remote content, and absence of an approved owner.
For each result, list the administrative evidence that would validate it and the follow-on endpoint and identity pivots required.
```
