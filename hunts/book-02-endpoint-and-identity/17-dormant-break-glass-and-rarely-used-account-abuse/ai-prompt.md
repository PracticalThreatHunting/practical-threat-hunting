# AI-Assisted Analysis Prompt — Hunt 17

Use only data approved for the selected AI system. Treat the output as proposed analysis and verify every material claim against the underlying evidence.

```text
Review these rarely used accounts using the supplied account inventory and authentication history.
Keep human, service, workload, guest, shared, and emergency accounts separate.
Do not equate missing sign-in events with inactivity.
Rank recent use by privilege, purpose, context novelty, and actions performed.
For each high-priority account, list the lifecycle records and log sources that must be verified.
```
