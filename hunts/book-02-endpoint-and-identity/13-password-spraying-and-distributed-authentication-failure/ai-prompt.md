# AI-Assisted Analysis Prompt — Hunt 13

Use only data approved for the selected AI system. Treat the output as proposed analysis and verify every material claim against the underlying evidence.

```text
Analyze this authentication aggregate as a possible password-spray campaign.
Keep each result code and authentication protocol distinct.
Compare many-user-per-source and many-source-per-user patterns.
Identify later successes and list the exact fields connecting them to the failures.
Explain shared-egress, stale-password, mobile-client, and security-testing alternatives.
Return observed facts, hypotheses, and recommended pivots in separate sections.
Do not infer that one person controlled multiple IP addresses without corroboration.
```
