# AI-Assisted Analysis Prompt — Hunt 05

Use only data approved for the selected AI system. Treat the output as proposed analysis and verify every material claim against the underlying evidence.

```text
Analyze these credential-access candidate events as a human-review queue.
For each event, list the source process, signer, path, account, target, access or behavioral event, created files, security-control changes, and subsequent authentications.
Separate approved diagnostic possibilities from adversary possibilities.
Do not state that credentials were obtained unless evidence shows successful collection or subsequent validated use.
Identify the identities whose credentials may be exposed and the containment decisions requiring human approval.
```
