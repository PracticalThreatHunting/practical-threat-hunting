# AI-Assisted Analysis Prompt — Hunt 02

Use only data approved for the selected AI system. Treat the output as proposed analysis and verify every material claim against the underlying evidence.

```text
Profile these dual-use binary events by binary, parent, target path, argument pattern, user role, device role, and destination.
For each uncommon combination, provide two explanations: a plausible legitimate workflow and a plausible abuse case.
List the specific file, signer, deployment, network, or change-management evidence needed to distinguish them.
Do not recommend allowlisting an entire binary, account, or management platform.
```
