-- Canonical teaching schema v1. Raw producer records remain beside it.
-- Nulls mean unavailable unless coverage_state/visibility_basis supplies a narrower reason.
CREATE TABLE events (
  event_id VARCHAR,
  fixture_id VARCHAR,
  case_id VARCHAR,
  control VARCHAR,
  source VARCHAR,
  event_type VARCHAR,
  ts TIMESTAMP,
  ts_end TIMESTAMP,
  sensor VARCHAR,
  src_ip VARCHAR,
  src_port INTEGER,
  dst_ip VARCHAR,
  dst_port INTEGER,
  transport VARCHAR,
  ip_proto INTEGER,
  direction VARCHAR,
  direction_basis VARCHAR,
  src_role VARCHAR,
  dst_role VARCHAR,
  src_segment VARCHAR,
  dst_segment VARCHAR,
  action VARCHAR,
  outcome VARCHAR,
  connection_state VARCHAR,
  service VARCHAR,
  app_proto VARCHAR,
  channel VARCHAR,
  query VARCHAR,
  base_domain VARCHAR,
  qtype VARCHAR,
  rcode VARCHAR,
  answer_count INTEGER,
  dns_label_length INTEGER,
  dns_label_entropy DOUBLE,
  sequential_label BOOLEAN,
  method VARCHAR,
  host VARCHAR,
  authority VARCHAR,
  uri VARCHAR,
  status_code INTEGER,
  user_agent VARCHAR,
  mime_type VARCHAR,
  filename VARCHAR,
  file_size BIGINT,
  file_hash VARCHAR,
  sni VARCHAR,
  alpn VARCHAR,
  tls_version VARCHAR,
  fingerprint VARCHAR,
  cert_subject VARCHAR,
  cert_issuer VARCHAR,
  cert_valid_days INTEGER,
  cert_self_signed BOOLEAN,
  sni_cert_match BOOLEAN,
  ech_status VARCHAR,
  quic_version VARCHAR,
  proxy_method VARCHAR,
  policy VARCHAR,
  vpn_session_id VARCHAR,
  assigned_ip VARCHAR,
  route_mode VARCHAR,
  mapping_state VARCHAR,
  bytes_out BIGINT,
  bytes_in BIGINT,
  packets_out INTEGER,
  packets_in INTEGER,
  duration_s DOUBLE,
  sample_rate INTEGER,
  counter_layer VARCHAR,
  flow_complete BOOLEAN,
  parser_zeek VARCHAR,
  parser_suricata VARCHAR,
  correlation_key VARCHAR,
  anomaly VARCHAR,
  icmp_version INTEGER,
  icmp_type INTEGER,
  icmp_code INTEGER,
  payload_len INTEGER,
  icmp_id INTEGER,
  icmp_seq INTEGER,
  ip_protocol_number INTEGER,
  tunnel_type VARCHAR,
  share_name VARCHAR,
  rpc_operation VARCHAR,
  remote_admin_proto VARCHAR,
  coverage_state VARCHAR,
  visibility_basis VARCHAR,
  approved BOOLEAN,
  proxy_rewrite BOOLEAN,
  collection_interval VARCHAR,
  note VARCHAR
);
