# Edition evidence

Executed locally September 9, 2026: 58 explicit cases and 12 contract checks pass; 9 reference mutants killed; 15 local policy controls pass; 90 bounded static checks pass; 9 focused adapter/output checks pass. Sigma check reported zero errors and issues; conversion versions and outputs are retained. Benchmark is five warm Python reference trials only.

Native KQL/SPL execution, scheduled replay, target resource acceptance, ES output materialization, native performance, native rollback, signature verification, and production deployment: NOT RUN/BLOCKED. No native or independent human approval is claimed.

The commit containing this directory binds these edition records to source. Re-running local commands writes build/, leaving edition evidence unchanged. Build content excludes docs/evidence to avoid volatile execution timestamps in deterministic content archives.
